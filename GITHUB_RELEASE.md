
# Logseq Text Toolkit v1.1.2

## 🐛 修复
- **修复嵌套格式处理问题**：修复了包含格式标记的混合内容被错误包裹引号的问题
- **优化引号包裹逻辑**：当文本包含 hiccup 格式片段时，不会在整个字符串外层错误地包裹引号

## 📝 修复示例
**修复前**（错误）：
```
输入: 数组越大 → **扫描越久 → STW（暂停时间）越长 → 服务卡顿**。
输出: [:span.blue "数组越大 → [:b \"扫描越久 → STW（暂停时间）越长 → 服务卡顿\"]。"]
```

**修复后**（正确）：
```
输入: 数组越大 → **扫描越久 → STW（暂停时间）越长 → 服务卡顿**。
输出: [:span.blue 数组越大 → [:b "扫描越久 → STW（暂停时间）越长 → 服务卡顿"]。]
```

## ✅ 测试验证
- 通过 51/52 个测试（通过率 98.08%）
- 所有嵌套格式处理相关的测试全部通过

## 📚 文档更新
- 新增思维视图为 Block View 的第 5 种视图类型
- 思维视图与列表、表格、画廊、看板同级
- 支持点击 Heatmap 的月份和周数创建对应日期页面

---

**完整变更记录**: [查看 diff](https://github.com/duiliuliu/logseq-text-toolkit/compare/f0c8c35...c1fd083)
