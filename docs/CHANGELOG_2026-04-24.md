# 更新日志 - 2026-04-24

## 功能优化

1. **图标系统优化**
   - 将插件图标从 SVG 替换为 tabler Webfont 网页字体
   - 主工具栏图标更换为 `ti ti-text-wrap`
   - 所有工具栏项目图标都改为使用 tabler 图标类名
   - 提高了图标在不同主题下的显示一致性

2. **代码结构优化**
   - 修改了 `Toolbar` 组件的 `renderIcon` 函数，支持 tabler 图标类名
   - 简化了 `defaultSettings.json` 文件的图标配置
   - 提高了代码的可维护性和可读性

## 修复问题

1. **图标显示问题**
   - 解决了 SVG 图标在不同主题下显示不一致的问题
   - 确保 tabler 图标在深色模式下正确显示

2. **设置模态框背景问题**
   - 检查了设置模态框在深色模式下的背景设置
   - 确保模态框在深色模式下有合适的背景颜色

## 技术更新

1. **依赖更新**
   - 支持 tabler Webfont 网页字体图标系统
   - 提高了图标的一致性和可定制性

2. **代码质量**
   - 优化了 `Toolbar` 组件的图标渲染逻辑
   - 确保代码符合 TypeScript 类型定义

## 提交信息

- 提交了所有更改到 `dev-text-tool` 分支
- 更新了文档和配置文件
- 确保代码的健壮性和可维护性