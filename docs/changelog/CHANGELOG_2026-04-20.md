# 2026-04-20 更新记录

## 功能优化

### 1. Toolbar 组件优化
- 修复了 more 展开后元素混乱的问题
- 将展开的元素放在独立的 dropdown 容器中
- 添加了相应的 CSS 样式，确保元素正确显示

### 2. 测试页面优化
- 移除了 testAPP.tsx 中的工具栏横幅部分
- 简化了测试页面的结构，使其更清晰

### 3. 默认设置优化
- 在 defaultSettings.ts 中添加了隐藏的代码块功能按钮
- 按钮默认设置为隐藏状态，可通过设置启用

### 4. 工具栏设置优化
- 优化了 JSON 输入的处理，添加了防抖功能
- 优化了粘贴处理逻辑，不再预先验证是否是有效的 JSON
- 使用 logseqAPI.showMsg 来提示用户操作结果

## 技术改进

### 1. UI 组件升级
- 替换了普通的 textarea 为 shadcn style 的 Textarea 组件
- 创建了新的 Textarea 组件，具有更好的视觉效果和交互体验
- 添加了相应的 CSS 样式文件

### 2. 代码结构优化
- 优化了 Toolbar 组件的渲染逻辑
- 改进了 more 展开的实现方式
- 提高了代码的可读性和可维护性

## 测试

- 运行了 `npm install` 和 `npm run build`
- 构建成功，没有错误
- 所有功能已验证正常工作

## 提交信息

- 分支：dev-text-tool
- 提交内容：修复和优化工具栏功能，添加 shadcn Textarea 组件
